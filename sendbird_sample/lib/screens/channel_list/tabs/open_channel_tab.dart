import 'package:flutter/material.dart';

class OpenChannelTab extends StatelessWidget {
  const OpenChannelTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("오픈 탭"),
    );
  }
}
